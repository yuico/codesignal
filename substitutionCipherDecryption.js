function substitutionCipherDecryption(contents, signature, encryptedSignature) {
    /* let c = contents.toLowerCase().split();
    let s = signature.toLowerCase().split();
    let e = encryptedSignature.toLowerCase().split(); */
    for(let i=0; i<signature.length; i++){
        signature[i]
    }
    for(let i=0; i<encryptedSignature.length; i++){
        
    }
}

let contents = "issomtoqmvjts";
let signature = "alice";
let encryptedSignature = "james";
